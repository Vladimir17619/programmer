x = prompt();
y = prompt();
function sum(x, y) {
    return (x + y)
}
z = sum(x, y);
alert(z);
function dif(x, y) {
    return (x - y)
}
z = dif(x, y);
alert(z);
function mul(x, y) {
    return (x * y)
}
z = mul(x, y);
alert(z);
function div(x, y) {
    return (x / y)
}
z = div(x, y);
alert(z);
